name: geofileops-installed-latest
channels:
  - conda-forge
dependencies:
  - python
  - pip
  # required
  - geofileops
  # optional
  - pyarrow-core
  - simplification
  # testing
  - pytest
  - pytest-xdist