name: geofileopsdev
channels:
  - conda-forge
dependencies:
  - python =3.9
  - pip
  # required
  - cloudpickle
  - fiona
  - gdal
  - geopandas >=0.12
  - numpy
  - pandas
  - psutil
  #- pygeoops >=0.3,<0.4
  - pyogrio
  - pyproj
  - shapely >=2,<2.1
  - topojson
  # docs
  # Restrict sphinx version to < 6 to avoid logo error
  - sphinx<6
  - pydata-sphinx-theme
  - pip:
    - pygeoops ==0.3.0a0
    - sphinx-automodapi ==0.13
    - simplification
